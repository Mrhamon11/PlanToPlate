# Task 1.1: Core Project Initialization & Layout

## Goal
Initialize the Django project environment, optimize SQLite for production-like concurrency, and establish a clean, mobile-responsive global UI shell using semantic base templates.

## Context Needed
* `PLAN.md`
* `AGENT.md`

## Step-by-Step Instructions
1. Initialize a Django 5.x project named `plantoplate`. Create a core app named `core`.
2. Configure `settings.py` to utilize environment variables for security keys. Set up database configuration to enforce standard WAL mode adjustments on SQLite connections for safe multi-user concurrency.
3. Create a global `base.html` template. Include HTMX via CDN or static asset vendor folder. Ensure it utilizes mobile-first viewports and clean semantic CSS layouts.
4. Establish basic unit tests ensuring the root homepage view returns a successful `200 OK` code.

## Verification Checklist
* [ ] Running server initializes without errors.
* [ ] Database file creates correctly and runs PRAGMA checks successfully.
* [ ] Target page responds seamlessly over HTMX requests.

## Technical API Log (To be filled out by Agent post-completion)